version: '3.8'

services:

  backend:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
  
  frontend:
    build: ./dashboard
    ports:
      - "3000:3000"
  
  db:
    image: postgres:14
    .env_file:
      - .env
    ports:
      - '5432:5432'